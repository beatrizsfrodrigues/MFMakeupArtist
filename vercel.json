{
  "rewrites": [{ "source": "/(.*)", "destination": "/app/dist/index.html" }],
  "builds": [
    {
      "src": "app/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "app/dist"
      }
    }
  ]
}
