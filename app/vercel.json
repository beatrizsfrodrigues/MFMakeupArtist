{
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "builds": [
    {
      "src": "app/package.json",
      "use": "@vercel/node"
    }
  ],
  "scripts": {
    "build": "cd app && npm install && npm run build"
  }
}
